# Клиент–Сервер на F# с чтением файла и логированием

Данный проект демонстрирует простую модель взаимодействия *клиент–сервер* на языке **F#**.
Особенностью реализации является то, что:

- **Клиент** считывает строки из входного файла `client_input.txt` и отправляет их на сервер.
- **Сервер** принимает сообщения, выводит их на экран и **записывает каждую строку в файл лога `server_log.txt`**.
- Обмен данными происходит через **TCP-сокет**.
- Программа полностью совместима с `dotnet fsi`, поэтому код выполняется напрямую без функции `main`.

---

## Структура проекта
server.fsx
client.fsx
client_input.txt
server_log.txt (создаётся автоматически)

---

## 🖥️ Описание работы

### Сервер (`server.fsx`)
Сервер:

1. Создаёт TCP-слушатель на порту **5000**.  
2. Принимает подключение клиента.  
3. Для каждой полученной строки:
   - выводит её в консоль,
   - дописывает строку в файл `server_log.txt`,
   - отправляет клиенту подтверждение `"OK"`.  

Сервер работает в бесконечном цикле и способен принимать подключение за подключением.

---

### Клиент (`client.fsx`)
Клиент:

1. Считывает строки из файла **`client_input.txt`**.  
2. Подключается к серверу по адресу `127.0.0.1:5000`.  
3. Отправляет на сервер каждую строку из файла.  
4. После отправки получает от сервера ответ `"OK"` и выводит его на экран.

---

## 📄 Формат входного файла клиента (`client_input.txt`)

Файл `client_input.txt` является источником данных для клиента.  
Каждая строка файла интерпретируется как отдельное сообщение, которое будет отправлено на сервер.

### 🔹 Требования к формату:

1. **Каждая строка — одно сообщение.**  
   Строки передаются на сервер в том порядке, в котором они находятся в файле.

2. **Пустые строки игнорируются.**

3. **Кодировка файла должна быть UTF-8.**

4. **Допустимое содержимое строки:**
   - произвольный текст,
   - слова,
   - предложения,
   - числа,
   - любые пользовательские данные, которые требуется отправить на сервер.

### 🔹 Пример файла `client_input.txt`:


Hello server
How a beautifuk day!
F# is amazing!

