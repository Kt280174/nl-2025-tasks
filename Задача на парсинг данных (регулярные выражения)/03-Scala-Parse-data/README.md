# Access Log Analyzer (Scala)

Проект представляет собой **анализатор логов веб-сервера**  
(формат `access.log`), реализованный на языке **Scala**.

Программа решает практическую задачу из области:

> **Парсинг текстовых данных с использованием регулярных выражений**

и демонстрирует, как с помощью **Scala Regex**
(`scala.util.matching.Regex`) можно разбирать строки сложного формата
и преобразовывать их в удобные структурированные объекты.

---

## 📌 Формат входных данных

Входные данные хранятся в файле `input.txt`.

Каждая строка должна соответствовать формату, близкому
к логам веб-серверов **Apache / Nginx (access.log)**.

Одна строка содержит:

- IP-адрес клиента  
- дату и время запроса  
- HTTP-метод (`GET`, `POST`, `PUT`, …)  
- путь запроса (`/api/users`, `/login`, …)  
- протокол (`HTTP/1.1`)  
- код статуса ответа (`200`, `404`, `500`, …)  
- количество отправленных байт  
- `Referer`  
- `User-Agent`


