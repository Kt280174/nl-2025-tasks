# Access Log Analyzer (D Language)

Проект представляет собой **анализатор логов веб-сервера** (формат access.log), 
написанный на языке **D**.  
Программа демонстрирует практическую задачу из категории:

> **Парсинг данных (регулярные выражения)**  

и показывает, как с помощью `std.regex` можно эффективно разбирать строки 
сложного формата и извлекать из них структурированную информацию.

---

## 📌 Формат входных данных

Файл `input.txt` должен содержать строки в формате, близком к Apache / Nginx access log:

Каждая строка содержит:

- IP-адрес клиента  
- дату и время  
- HTTP-метод (GET, POST …)  
- путь (`/api/users`, `/login`, …)  
- протокол  
- статус ответа (200, 404, 500 …)  
- количество отправленных байт  
- User-Agent  

Некорректные строки пропускаются.

