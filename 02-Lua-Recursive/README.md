# 📘 Анализ максимальной глубины структуры по отступам (Lua)

## 🔍 Описание
Эта программа вычисляет **максимальную глубину дерева**, заданного с помощью строк с отступами.  
Каждый уровень вложенности соответствует **2 пробелам**.  
Алгоритм использует **рекурсивный обход**, чтобы определить глубину всех подузлов.

Функция `compute_depth` вызывает саму себя для анализа всех строк, которые принадлежат более глубокому уровню вложения.

---

## Формат входного файла

Имя файла: **input_lua.txt**

Структура задаётся в виде строк, где:

- каждый уровень = **2 пробела**
- строки без пробелов — уровень 0
- вложенность определяется только количеством пробелов

### Пример входных данных: 
input_lua.txt:
root
  folderA
    file1.txt
    file2.txt
  folderB
    folderC
      file3.txt


