# Клиент-серверное приложение на Objective-C с обменом JSON

Данный проект представляет собой **простое клиент-серверное приложение**, написанное на **Objective-C** с использованием низкоуровневых BSD-сокетов и библиотеки Foundation.

Код объединён в **одну программу**:

- сервер запускается в отдельном потоке (`pthread`) внутри той же программы;
- клиентская часть читает JSON из файла, отправляет его на сервер по TCP-сокету и получает ответ;
- данные обмениваются в формате **JSON**.

По типу задания это явно относится к категории:

> 🧩 **«Клиент-серверное взаимодействие (клиент и сервер могут быть написаны на разных языках)»**

---

## 📌 Общая идея

1. В функции `main`:
   - создаётся отдельный поток, в котором запускается сервер `runServer`;
   - после небольшой паузы (`sleep(1)`) клиент читает JSON из файла `request.json`;
   - клиент устанавливает TCP-соединение с `127.0.0.1:5000`, отправляет JSON на сервер и ждёт ответ;
   - ответ сервера снова парсится как JSON и выводится в лог.

2. В функции `runServer`:
   - создаётся TCP-сервер на порту `5000`;
   - сервер ждёт подключения клиента (`accept`);
   - читает JSON-строку из сокета;
   - парсит JSON с помощью `NSJSONSerialization`;
   - в зависимости от поля `"operation"` (`"sum"`, `"avg"`, `"max"`) вычисляет результат по массиву `"numbers"`;
   - отправляет клиенту JSON с полем `"result"`.

---

## 📂 Формат входных данных (`request.json`)

Файл `request.json`:
Ожидаемый формат:

{
  "operation": "sum",
  "numbers": [1, 2, 3, 4.5]
}


Поддерживаются три операции:

- "sum" — сумма всех чисел;

- "avg" — среднее значение;

- "max" — максимальное значение.